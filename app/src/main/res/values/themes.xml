<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CinemaHub" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item> <!-- Make the status bar icons dark for better contrast -->
    </style>
    <style name="ToolbarTitleBold">
        <item name="android:textStyle">bold</item>
    </style>
    <style name="Theme.CinemaHub" parent="Base.Theme.CinemaHub" />
</resources>
